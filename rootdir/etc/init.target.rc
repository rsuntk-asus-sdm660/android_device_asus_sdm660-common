# Copyright (c) 2013-2018, 2020, 2021 The Linux Foundation. All rights reserved.

on early-init
    wait /dev/msm-rng 2
    # hw_random создаётся системой, symlink/chmod/chown не нужны
    # symlink /dev/msm-rng /dev/hw_random
    # chmod 0644 /dev/hw_random
    # chown root root /dev/hw_random
    write /sys/module/msm_thermal/parameters/enabled N

    exec u:r:vendor_modprobe:s0 -- /vendor/bin/modprobe -a -d /vendor/lib/modules audio_apr audio_wglink audio_adsp_loader audio_q6_pdr audio_q6_notifier audio_q6 audio_usf audio_native audio_pinctrl_wcd audio_pinctrl_lpi audio_swr audio_platform audio_hdmi audio_wcd_spi audio_stub audio_digital_cdc audio_analog_cdc audio_msm_sdw audio_wcd_cpe audio_cpe_lsm audio_wcd9335 audio_machine_sdm660 audio_wcd934x audio_mbhc audio_wcd9xxx audio_wcd_core

on init
    # Schedtune baseline (умеренный)
    write /dev/stune/foreground/schedtune.sched_boost_no_override 1
    write /dev/stune/top-app/schedtune.sched_boost_no_override 1
    write /dev/stune/schedtune.colocate 0
    write /dev/stune/background/schedtune.colocate 0
    write /dev/stune/system-background/schedtune.colocate 0
    write /dev/stune/foreground/schedtune.colocate 0
    write /dev/stune/top-app/schedtune.colocate 1

    # ★ ИЗМЕНЕНО: Умеренный начальный boost
    write /dev/stune/top-app/schedtune.boost 5

on fs
    wait /dev/block/platform/soc/${ro.boot.bootdevice}
    symlink /dev/block/platform/soc/${ro.boot.bootdevice} /dev/block/bootdevice
    start hwservicemanager
    mount_all --early

    chown root system /mnt/vendor/persist
    chmod 0771 /mnt/vendor/persist
    restorecon_recursive /mnt/vendor/persist
    mkdir /mnt/vendor/persist/data 0700 system system
    mkdir /mnt/vendor/persist/bms 0700 root system
    write /sys/kernel/boot_adsp/boot 1

    chown system system /sys/devices/platform/soc/a800000.ssusb/mode

    mkdir /mnt/vendor/dsp 0770 root root
    copy /vendor/dsp/cdsp/fastrpc_shell_3 /mnt/vendor/dsp/fastrpc_shell_3
    chmod 0644 /mnt/vendor/dsp/fastrpc_shell_3
    mount none /mnt/vendor/dsp/fastrpc_shell_3 /vendor/dsp/cdsp/fastrpc_shell_3 bind

on late-fs
    mount_all --late

on post-fs
    write /dev/ipa 1
    start vendor.qseecomd
    wait_for_prop vendor.sys.listeners.registered true

on early-boot
    chown system system /mnt/vendor/persist/sensors
    chown system system /mnt/vendor/persist/sensors/sns.reg
    chown system system /mnt/vendor/persist/sensors/sensors_settings
    chmod 0664 /mnt/vendor/persist/sensors/sensors_settings

on post-fs-data
    mkdir /data/vendor/nfc 0770 nfc nfc

on boot
    start vendor.rmt_storage

    setprop persist.camera.gyro.disable 0
    mkdir /data/vendor/camera 0770 camera camera

    chown wifi wifi /sys/module/wlan/parameters/fwpath

    setprop vendor.usb.rndis.func.name "rndis_bam"
    setprop vendor.usb.rmnet.func.name "rmnet_bam"
    setprop vendor.usb.rmnet.inst.name "rmnet"
    setprop vendor.usb.dpl.inst.name "dpl"
    setprop vendor.usb.qdss.inst.name "qdss"

service vendor.per_mgr /vendor/bin/pm-service
    class core
    user system
    group system
    ioprio rt 4

service vendor.per_proxy /vendor/bin/pm-proxy
    class core
    user system
    group system
    disabled

on property:init.svc.vendor.per_mgr=running
    start vendor.per_proxy

on property:sys.shutdown.requested=*
    stop vendor.per_proxy

service vendor.qseecomd /vendor/bin/qseecomd
    class core
    user root
    group root

service vendor-sensor-sh /vendor/bin/init.qcom.sensors.sh
    class core
    user root
    oneshot

service vendor.sensors.qti /vendor/bin/sensors.qti
    class core
    user system
    group system
    disabled

service vendor.pd_mapper /vendor/bin/pd-mapper
    class core

on property:vold.decrypt=trigger_restart_framework
    restart vendor.camera-provider-2-4

service dcvs-sh /vendor/bin/init.qti.dcvs.sh
    class late_start
    user root
    group root system
    disabled
    oneshot

on property:vendor.dcvs.prop=1
    start dcvs-sh

on charger
    start vendor.qcom-post-boot
    start vendor.hvdcp_opti
    start vendor.power_off_alarm

service vendor.hvdcp_opti /vendor/bin/hvdcp_opti
    class main
    user root
    group system wakelock

# ═══════════════════════════════════════════════════════════════════════════
# Boot completed — thermal caps HAL handles performance, not this script
# ═══════════════════════════════════════════════════════════════════════════
on property:sys.boot_completed=1
    # ★ ИЗМЕНЕНО: Не сбрасываем boost — thermal HAL управляет caps
    # Только логируем завершение загрузки
    write /dev/kmsg "Boot completed - thermal caps active"
